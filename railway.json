{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3-pip python3.9-dev build-essential ffmpeg libffi-dev libjpeg-dev libopus0 libopus-dev opus-tools libcairo2 pkg-config libpq-dev && pip3 install numpy==1.21.6 && pip3 install --no-cache-dir -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python3 -m FallenMusic",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "healthcheckInterval": 15
  }
} 